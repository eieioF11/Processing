{
    "version": "2.0.0",
    "command": "processing-java",
    "args": [
        "--force",
        "--sketch=${workspaceRoot}",
        "--output=${workspaceRoot}\\out",
        "--run"
    ],
    "tasks": [
        {
            "label": "processing-java",
            "type": "shell",
            "command": "processing-java",
            "args": [
                "--force",
                "--sketch=${workspaceRoot}",
                "--output=${workspaceRoot}\\out",
                "--run"
            ],
            "problemMatcher": [],
            "group": "build"
        }
    ]
}